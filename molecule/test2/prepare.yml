---
- name: Prepare
  hosts: all
  gather_facts: false
  become: true
  tasks:

    #- name: Install Python3.11
    #  ansible.builtin.apt:
    #    name: python3.11
    #    state: present

    - name: Install pip packages
      ansible.builtin.pip:
        requirements: requirements.txt
        state: present

    #- name: Install hczv.keepass ansible collection
    #  community.general.ansible_galaxy_install:
    #    name: hczv.keepass
    #    type: collection
